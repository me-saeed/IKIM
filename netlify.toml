# Netlify build config for IKIM Voice (Next.js)
# Backend API URL is baked into the client at build time (NEXT_PUBLIC_*).
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  # Backend API – change if you use a different host
  NEXT_PUBLIC_API_URL = "https://stranger.encodersoft.dev"
