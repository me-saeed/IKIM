# Server
PORT=4000
NODE_ENV=development

# OpenAI (GPT + Whisper)
OPENAI_API_KEY=sk-your-openai-key-here

# Prisma – Supabase Postgres (required)
# Replace [YOUR-PASSWORD] with your DB password. If it contains $ # / @ etc., URL-encode:
#   $ → %24   # → %23   / → %2F   @ → %40
#
# Pooler (runtime) – use in DATABASE_URL:
DATABASE_URL="postgresql://postgres.pmtcpsdoljsmncnlzyjj:[YOUR-PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
#
# Direct (migrations / db push) – MUST use db.PROJECT_REF.supabase.co, not the pooler.
# User is "postgres" (no .project-ref). Get this from Dashboard → Connect → "Direct connection".
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.pmtcpsdoljsmncnlzyjj.supabase.co:5432/postgres?sslmode=require"
